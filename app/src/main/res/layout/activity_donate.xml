<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="10dp"
    tools:context=".DonateActivity">

    <TextView
        android:id="@+id/donateIntroText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:padding="16dp"
        android:text="This page does not show every time you use the application. "
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent" />

    <ImageView
        android:id="@+id/feedthecatImage"
        android:layout_width="300dp"
        android:layout_height="175dp"
        android:scaleType="fitXY"
        android:src="@drawable/feedthecat"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toBottomOf="@id/donateIntroText"/>

    <TextView
        android:id="@+id/donateText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:padding="16dp"
        android:text="Please make a small financial gift to support my software projects, and help feed the cat. See also menu option top right - help feed the cat."
        app:layout_constraintTop_toBottomOf="@id/feedthecatImage" />

    <TextView
        android:id="@+id/donateClickText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:padding="16dp"
        android:text="Click here ->"
        app:layout_constraintTop_toBottomOf="@id/donateText"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintRight_toLeftOf="@id/donateButton" />

    <Button
        android:id="@+id/donateButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="FEED\nTHE CAT"
        app:layout_constraintTop_toBottomOf="@id/donateText"
        app:layout_constraintLeft_toRightOf="@id/donateClickText"
        app:layout_constraintRight_toLeftOf="@id/donateContinueButton" />

    <ImageButton
        android:id="@+id/donateContinueButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:contentDescription="touch to move to application"
        android:src="@android:drawable/ic_media_play"
        app:layout_constraintLeft_toRightOf="@id/donateButton"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toBottomOf="@id/donateText" />

    <TextView
        android:id="@+id/donateNextText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:padding="16dp"
        android:text="To continue. Click play --^^"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toBottomOf="@id/donateButton" />

</androidx.constraintlayout.widget.ConstraintLayout>